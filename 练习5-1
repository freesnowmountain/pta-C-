本题要求实现一个计算m~n（m<n）之间所有整数的和的简单函数。

函数接口定义：
int sum( int m, int n );
其中m和n是用户传入的参数，保证有m<n。函数返回的是m~n之间所有整数的和。
//前几个比较简单，没什么好说的。
答案：
int sum(int m, int n){
    int i,sum=0;
    for(i=m;i<=n;i++){
        sum=sum+i;
    }
    return sum;
}
